/**
 * Monolithic function for running through API request and setting expected data to state
 * @param {string} call Input which wordpress api call to make between: pages, guildname, banner, logo, post
 */

export default function fetchWordpress(call) {
    console.log('Function successfully exported and called');
}